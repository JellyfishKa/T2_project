# MVP Announcement Templates

---

## Вариант 1: Slack / Telegram (короткий)

```
🚀 T2 MVP Ready!

AI-платформа оптимизации маршрутов розничной сети — MVP готов к демо.

Что работает:
• Оптимизация маршрутов через 2 LLM-модели (Qwen + Llama) с авто-fallback
• Dashboard, аналитика, бенчмаркинг моделей
• 12 API-эндпоинтов, 4 UI-страницы
• Docker Compose — запуск одной командой

📋 Демо: [дата и время]
🔗 Доступ: http://[SERVER_IP] (фронтенд) | http://[SERVER_IP]:8000/docs (API)
📖 Документация: docs/MVP_DEMO_SCRIPT.md

Вопросы → [контактное лицо]
```

---

## Вариант 2: Email (подробный)

**Тема:** T2 MVP 1.0 готов — AI-оптимизация маршрутов розничной сети

---

Уважаемые коллеги,

Рады сообщить, что MVP платформы T2 готов к демонстрации. T2 — это AI-powered система оптимизации маршрутов для розничных сетей, использующая локальные LLM-модели.

### Ключевые функции MVP

- **AI-оптимизация маршрутов** — генерация оптимального порядка обхода точек с учётом временных окон, приоритетов и ограничений
- **2 LLM-модели** — Qwen (основная) и Llama (fallback) с автоматическим переключением
- **Бенчмаркинг** — автоматическое сравнение моделей по скорости, качеству и надёжности
- **Веб-интерфейс** — Dashboard, страница оптимизации и аналитики
- **Полная инфраструктура** — Docker Compose, CI/CD, PostgreSQL, Redis

### Цифры

| Метрика | Значение |
|---------|----------|
| API-эндпоинтов | 12 |
| UI-страниц | 4 |
| LLM-моделей | 2 |
| Docker-сервисов | 4 |

### Доступ

- **Фронтенд:** `http://[SERVER_IP]`
- **Swagger API:** `http://[SERVER_IP]:8000/docs`
- **Документация:** `docs/` в репозитории

### Демонстрация

Демо запланировано на **[дата], [время]**.
Сценарий демо: `docs/MVP_DEMO_SCRIPT.md`

### Следующие шаги

- История маршрутов и загрузка данных из файлов
- Аутентификация и мониторинг
- Интеграция с более крупными LLM-моделями
- Production-деплой с HTTPS

Для вопросов и обратной связи обращайтесь к **[контактное лицо]**.

С уважением,
Команда T2

---

## Инструкция по использованию шаблонов

1. Замените `[SERVER_IP]` на реальный IP или домен сервера
2. Замените `[дата и время]` на дату и время демо
3. Замените `[контактное лицо]` на имя и контакт ответственного
4. Для Slack/Telegram — скопируйте Вариант 1
5. Для Email — скопируйте Вариант 2 и вставьте в письмо
